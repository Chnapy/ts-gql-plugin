// import { waitPromiseSync } from './wait-promise-sync';

describe('Wait promise sync', () => {
  it('waits promise end before giving result', () => {
    // console.log(require.resolve('deasync'));
    // const result = waitPromiseSync(new Promise((r) => r()).then(() => 12));
    // expect(result).toEqual(12);
  });
});
