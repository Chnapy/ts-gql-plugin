scalar DateTime

type User {
  id: ID!
  oauthId: String!
  email: String!
  name: String!
  picture: String
  createdAt: DateTime
}

type Query {
  users: [User!]!
  user(id: ID!): User!
  toto(id: ID!): User!
}
